export async function onRequestPost(context){const b=await context.request.json();const sessionId=String(b.session_id||"").trim();if(!sessionId)return json({error:"missing session_id"},400);await context.env.DB.prepare(`UPDATE sessions SET completed=1,updated_at=? WHERE session_id=?`).bind(new Date().toISOString(),sessionId).run();return json({ok:true})}function json(data,status=200){return new Response(JSON.stringify(data),{status,headers:{"Content-Type":"application/json; charset=utf-8"}})}